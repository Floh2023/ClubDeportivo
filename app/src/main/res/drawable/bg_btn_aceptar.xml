<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#ffffff" /> <!-- Color del botón -->
    <corners android:radius="12dp" /> <!-- Bordes redondeados -->
    <stroke
        android:width="1dp"
        android:color="#000000" />
</shape>